# Example Postgres connection using DATABASE_URL
DATABASE_URL=postgres://dev:dev@localhost:5432/devdb

# Alternatively, specify PG_* variables
PGHOST=localhost
PGPORT=5432
PGUSER=dev
PGPASSWORD=dev
PGDATABASE=devdb

NODE_ENV=development
PORT=5000

# Copy this file to .env and edit values for your environment